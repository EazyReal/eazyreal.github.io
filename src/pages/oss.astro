---
import ArticleLayout from "../layouts/ArticleLayout.astro";
import { ossGroups, pageMeta } from "../data/siteContent";

const frontmatter = pageMeta.oss;
---

<ArticleLayout frontmatter={frontmatter}>
  <p>
    Selected work from 30+ merged upstream PRs to RL, agent infrastructure, and LLM serving systems. I was
    <a href="https://github.com/areal-project/AReaL/issues/1652">nominated for AReaL maintainership</a>.
  </p>

  {
    ossGroups.map((group) => (
      <>
        <h2 id={group.id}>{group.name}</h2>
        <ul>
          {
            group.contributions.map((contribution) => (
              <li>
                <a href={contribution.href}>{contribution.title}</a> - {contribution.description}
              </li>
            ))
          }
        </ul>
      </>
    ))
  }
</ArticleLayout>
